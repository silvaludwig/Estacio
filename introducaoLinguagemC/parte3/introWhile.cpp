#include <stdio.h>

int main(){
    int i = 1; //variável de incremento

    while (i <= 5) //enquanto i não for menor ou igual a 5, o programa vai executar o código
    {
        printf(" %d\n", i); //imprime o valor de i
        i++; // incrementa 1 ao valor de i
    }
    
    return 0;
}